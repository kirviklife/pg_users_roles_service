@model APP_PG_USERS_ROLES_SERVICE.Models.not_typical_grants_exec

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>not_typical_grants_exec</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id_not_typical_grant_exec" />
            <div class="form-group">
                <label asp-for="not_typical_grant_id" class="control-label"></label>
                <select asp-for="not_typical_grant_id" class="form-control" asp-items="ViewBag.not_typical_grant_id"></select>
                <span asp-validation-for="not_typical_grant_id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="date_time_exec" class="control-label"></label>
                <input asp-for="date_time_exec" class="form-control" />
                <span asp-validation-for="date_time_exec" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="is_success" /> @Html.DisplayNameFor(model => model.is_success)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
