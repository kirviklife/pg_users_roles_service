@model IEnumerable<APP_PG_USERS_ROLES_SERVICE.Models.users_roles_relation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.date_time_excec)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.roles1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.roles2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.date_time_excec)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.roles1.id_role)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.roles2.id_role)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_users_roles_relation">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_users_roles_relation">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_users_roles_relation">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
