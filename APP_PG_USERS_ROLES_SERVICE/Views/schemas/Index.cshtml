@model IEnumerable<APP_PG_USERS_ROLES_SERVICE.Models.schemas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.schm_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.oid_schm)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.databases)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.schm_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.oid_schm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.databases.id_db)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_schm">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_schm">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_schm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
