@model IEnumerable<APP_PG_USERS_ROLES_SERVICE.Models.servers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ipadd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.srv_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.port_srv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ipadd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.srv_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.username)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.port_srv)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_srv">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_srv">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_srv">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
